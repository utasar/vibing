; Input: R0 = string address
; Output: R1 = length
STRLEN
    AND R1, R1, #0
    ADD R2, R0, #0
STRLEN_LOOP
    LDR R3, R2, #0
    BRz STRLEN_DONE
    ADD R1, R1, #1
    ADD R2, R2, #1
    BRnzp STRLEN_LOOP
STRLEN_DONE
    RET
